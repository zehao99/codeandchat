import React from "react";

/**
 * Create context to record database handle and auth information.
 */

export const AuthContext = React.createContext();
export const FirestoreContext = React.createContext();
